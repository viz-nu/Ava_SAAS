{
  "operationName": "CreateIntegration",
  "query": "mutation CreateIntegration($name: String!, $purpose: String, $type: IntegrationTypeEnum!, $config: JSON) { createIntegration(name: $name, purpose: $purpose, type: $type, config: $config) { _id metaData { name type purpose } isActive createdAt } }",
  "variables": {
    "name": "Twilio Production",
    "purpose": "SMS and Voice",
    "type": "twilio",
    "config": {
      "AccountSid": "{{TWILIO_ACCOUNT_SID}}",
      "authToken": "{{TWILIO_AUTH_TOKEN}}",
      "state": "authorized"
    }
  },
  "_meta": { "auth": "Bearer {{ACCESS_TOKEN}}", "scope": "integration:create", "requiresSandbox": "Twilio test credentials" }
}
