{
  "operationName": "CreateCampaign",
  "query": "mutation CreateCampaign($name: String, $communicationChannels: [ID], $leads: [ID], $nodes: [JSON], $edges: [JSON]) { createCampaign(name: $name, communicationChannels: $communicationChannels, leads: $leads, nodes: $nodes, edges: $edges) { _id name createdAt } }",
  "variables": {
    "name": "Q1 Outbound Sales",
    "communicationChannels": ["{{CHANNEL_ID}}"],
    "leads": ["{{LEAD_ID_1}}", "{{LEAD_ID_2}}"],
    "nodes": [{ "id": "call", "type": "outboundCall" }],
    "edges": []
  },
  "_meta": { "auth": "Bearer {{ACCESS_TOKEN}}", "note": "Requires telephony channel (twilio/phone)" }
}
