{
  "operationName": "GetUploadUrl",
  "query": "mutation GetUploadUrl($key: String!) { getUploadUrl(key: $key) }",
  "variables": {
    "key": "{{BUSINESS_ID}}/test-documents/sample.pdf"
  },
  "_meta": {
    "auth": "Bearer {{ACCESS_TOKEN}}",
    "nextStep": "PUT binary to returned presigned URL, then use URL in 06-create-collection-file.json"
  }
}
