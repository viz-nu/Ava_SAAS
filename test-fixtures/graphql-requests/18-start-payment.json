{
  "operationName": "StartPayment",
  "query": "mutation StartPayment($planId: ID!, $gateway: String, $paymentType: String, $startDate: DateTime) { startPayment(planId: $planId, gateway: $gateway, paymentType: $paymentType, startDate: $startDate) { _id plan { _id name } gateway amount { value currency } createdAt } }",
  "variables": {
    "planId": "{{PLAN_ID}}",
    "gateway": "razorpay",
    "paymentType": "subscription",
    "startDate": "2026-05-21T00:00:00.000Z"
  },
  "_meta": { "auth": "Bearer {{ACCESS_TOKEN}}", "scope": "subscription:upgrade", "requiresSandbox": "Razorpay test mode" }
}
