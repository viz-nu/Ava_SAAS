{
  "operationName": "CreateChannel",
  "query": "mutation CreateChannel($input: ChannelInput!) { createChannel(input: $input) { _id name type status webhookUrl systemPrompt createdAt } }",
  "variables": {
    "input": {
      "name": "Website Chat Widget",
      "type": "web",
      "systemPrompt": "You are the web chat assistant for Test Company.",
      "isPublic": true,
      "config": {
        "widgetPosition": "bottom-right",
        "primaryColor": "#4F46E5"
      },
      "UIElements": {
        "showBranding": true,
        "placeholder": "Type your message..."
      }
    }
  },
  "_meta": { "auth": "Bearer {{ACCESS_TOKEN}}", "scope": "channel:create" }
}
