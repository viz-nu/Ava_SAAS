{
  "operationName": "Login",
  "query": "mutation Login($input: LoginInput!) { login(input: $input) { accessToken role scopes user { _id name email role business { _id name } } } }",
  "variables": {
    "input": {
      "email": "admin@test-company.com",
      "password": "TestPassword123!"
    }
  },
  "_meta": {
    "auth": "none",
    "description": "Authenticate verified user and obtain JWT access token"
  }
}
