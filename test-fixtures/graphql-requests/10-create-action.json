{
  "operationName": "CreateAction",
  "query": "mutation CreateAction($action: ActionInput!) { createAction(action: $action) { _id name description async needsApproval } }",
  "variables": {
    "action": {
      "name": "lookupOrderStatus",
      "description": "Look up order status by order ID",
      "async": false,
      "needsApproval": false,
      "parameters": {
        "type": "object",
        "properties": {
          "orderId": { "type": "string", "description": "Customer order ID" }
        },
        "required": ["orderId"]
      },
      "functionString": "async function lookupOrderStatus({ orderId }) { return { status: 'shipped', orderId }; }",
      "isPublic": false
    }
  },
  "_meta": { "auth": "Bearer {{ACCESS_TOKEN}}", "scope": "action:create" }
}
