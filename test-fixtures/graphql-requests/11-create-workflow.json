{
  "operationName": "CreateWorkflow",
  "query": "mutation CreateWorkflow($name: String, $nodes: JSON, $connections: JSON) { createWorkflow(name: $name, nodes: $nodes, connections: $connections) { _id name status createdAt } }",
  "variables": {
    "name": "Lead Qualification Flow",
    "nodes": {
      "start": { "id": "start", "type": "trigger", "label": "Conversation Start" },
      "qualify": { "id": "qualify", "type": "llm", "label": "Qualify Lead", "prompt": "Extract intent and budget from user message" },
      "handoff": { "id": "handoff", "type": "action", "label": "CRM Handoff" }
    },
    "connections": [
      { "from": "start", "to": "qualify" },
      { "from": "qualify", "to": "handoff", "condition": "qualified" }
    ]
  },
  "_meta": { "auth": "Bearer {{ACCESS_TOKEN}}", "scope": "workflow:create" }
}
